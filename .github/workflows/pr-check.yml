name: PR Check

on:
  pull_request:
    branches: [main]
    paths:
      - src/**
      - public/**
      - e2e/**
      - package.json
      - package-lock.json
      - next.config.mjs
      - postcss.config.mjs
      - playwright.config.ts
      - Dockerfile
      - .github/workflows/**

permissions:
  contents: read
  packages: write
  security-events: write
  actions: read

jobs:
  lint_and_test:
    name: Lint and Test
    uses: ./.github/workflows/lint-format.yml

  security_scan:
    name: Security Scan
    uses: ./.github/workflows/security-scan.yml

  e2e:
    name: E2E Tests
    uses: ./.github/workflows/e2e-tests.yml
    secrets: inherit

  build:
    name: Build Docker Image
    needs: [lint_and_test, security_scan, e2e]
    uses: ./.github/workflows/docker-build.yml
    with:
      environment: dev
      version: pr
      push: false
